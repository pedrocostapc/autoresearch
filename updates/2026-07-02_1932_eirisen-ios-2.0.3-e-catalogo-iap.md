# eirisen iOS — 2.0.3 pronto pro Archive + catálogo IAP montado (falta print/código)

App: Ei Risen (App Store 6772821195, bundle risentech.eirisen.app). Sessão de hoje (02/07).

## Publicando HOJE: versão 2.0.3 / build 15
- Bump feito em `ios/App/App.xcodeproj/project.pbxproj` (2.0.1→2.0.3, build 12→15) +
  `npm build` + `cap sync ios`. Commit `eac4e1f` na main + pushado.
- Traz as features web de main (papéis/telas por funcionário + ver financeiro).
- **Sem risco de rejeição:** o pull só tocou archetypes/notificações/evolution — a UI de
  billing/plano está INALTERADA desde a 2.0.2 (que a Apple já aprovou). Gates iOS intactos
  (PlanGrid/PlanHero return null; BalanceDeskAI→IapCreditPackages; add-ons escondidos).
- **SUBMETIDA 100% HEADLESS (sem Xcode)** — provei que dá pra publicar por CLI:
  `xcodebuild archive` (cloud signing via `-allowProvisioningUpdates` + a chave `.p8`) →
  `xcodebuild -exportArchive` com `destination: upload` (sobe direto pro ASC) → criar versão +
  whatsNew + anexar build + `reviewSubmissions`/`reviewSubmissionItems` + submit, tudo via API.
  Estado: **WAITING_FOR_REVIEW**. **Não precisa mais abrir o Xcode pra publicar este app.**
  (Só tem cert "Apple Development" no keychain; a distribuição foi resolvida na nuvem pelo allowProvisioningUpdates.)

## Catálogo IAP construído na Apple (NÃO vai nesta versão — vários dias)
- **5 recargas consumíveis** novas (`eirisen.credito.5000/10000/20000/40000/50000`, R$50-500,
  valor cheio sem bônus) + **12 add-ons assinatura** (grupos "Usuários adicionais"
  `eirisen.seat.1..6` e "Números adicionais" `eirisen.instance.1..6`). Preço/loc/BRA feitos via API.
- **TUDO em MISSING_METADATA.** Bloqueios: (1) 18 assinaturas precisam de PRINT de paywall
  VÁLIDO (Apple exige, não pode ser ícone/print da ficha — suporte confirmou por email);
  (2) código não wirado (appleIap.ts + apple-iap-validate não conhecem os 17 produtos; add-ons
  escondidos no iOS de propósito); (3) 5 recargas seguem travadas por motivo não-claro (escavar).
- Regra Apple aprendida: bônus de recarga é WEB/Stripe só; no iOS credita valor cheio (Apple leva 30%).

## Numeração de versão iOS (pegadinha)
"1.05"=[1,5]; sequência real 1.05→1.6→1.7→1.8.1→2.0→2.0.1→2.0.2→(2.0.3 agora). Próxima sempre > a última.

## Secrets
Segui a regra nova: nada do `.env.local`; ASC via chave `.p8` local; Supabase seria via Bitwarden
(secret ID no cofre) — não precisei do banco nesta sessão.

## ✅ RESULTADO (03/07 13:07): APROVADA E NO AR
2.0.3 (build 15) = **READY_FOR_SALE** — Apple aprovou sem rejeição em ~17h (submetida 02/07 19:50 → live 03/07 13:07). **Publicação 100% headless validada ponta-a-ponta** (bump→archive→upload→submit→aprovada, zero Xcode GUI). Monitor encerrado.
