# Balanço — Core (risencore) + frota — 2026-07-06 18:23

Sessão maratona pelo Core/broker, tocando também RisenOS, SuperSec e Hub. Foco: ligar
serviços externos reais e propagar padrões pela frota. Tudo via Core, stateless pra segredo.

## PASSADO (o que subiu no ar e foi provado)
- **Sicoob extrato/saldo NO AR** (Core, ref `hjclvuzugdbpvnomvtpn`, branch broker): espelho
  multibanco (`bank_credentials` / `bank_statement_entries` / `bank_statement_cursor` +
  view `v_extrato_entries` unindo Cora legado) + `bank-statement-sync-tick` (cron 15min).
  Cred Risen Mídia no cofre risencore (SICOOB_CLIENT_ID/CERT_PEM/KEY_PEM). Provado ao vivo:
  saldo R$12.212,71 + 13 lançamentos. Conta = 9034587 (com dígito).
- **Motor de boleto Sicoob** (Cobrança v3): `v1-provider-sicoob` ganhou emitir/consultar/baixar
  (`_shared/boleto-sicoob.ts`). Deployado. Emissão TRAVADA (ver bugs).
- **Tabela de preços multi-provider** `core_emissao_precos` + resolver (tenant→app→global).
- **Pedido CRM↔RisenOS**: RPC `create_crm_order` (pedido PENDENTE, sem caixa/pagamento) +
  `erp.pedido.criar`/`obter` no `v1-risenos`, registrado no broker. Provado E2E pelo broker.
- **Conectores (módulo opt-in)** em 3 apps: Hub (coluna `enabled` + gate menu), RisenOS
  (`tenant_connectors` + tela Conectores + Pedidos do CRM), SuperSec (`tenant_connectors` +
  ConectoresCard bidirecional). Padrão copiado da UI de Conectores do Claude.
- **Concierge Bridge** (Core, genérico): `concierge_actions` + 3 fns (inbox/pending/ack) +
  Realtime + `CONCIERGE_TOKEN` e `CORE_SERVICE_ROLE_KEY` no cofre financaspedro. Provado E2E.

**Melhorou:** adapter Sicoob (escopo/desembrulho/id corrigidos); motor de extrato virou
multibanco genérico (banco novo = 1 linha + adapter).

## O QUE APRENDEU
- Sicoob v4 REAL ≠ doc antiga: escopo `cco_consulta` (não cco_extrato/saldo); resposta
  embrulhada em `{resultado:{...}}`; `transactionId` é o id estável.
- `create_sale` do RisenOS é venda de PDV JÁ PAGA (caixa + `auth.uid`) — não serve pra
  pedido de CRM (nasce antes do pagamento). Precisou RPC nova `create_crm_order`.
- **SuperSec foi feito FORA do Lovable** → types do Supabase são ESTRITOS: `.from(tabela nova)`
  quebra o tsc ("assignable to never") até regenerar typegen. Hub/RisenOS (Lovable) = types soltos, passa.
- Conector ≠ plano: liga/desliga usa estado SEPARADO (`tenant_connectors`), não `tenant_modules`,
  pra não mexer no que o tenant paga.
- Core stateless reforçado (Concierge): fila guarda só o REGISTRO da aprovação; segredo no
  Bitwarden ao vivo; braço local executa.

## O QUE ERROU / BUGS
- **Chutei o `numeroCliente` de cobrança Sicoob** (734534=contrato, 4133=coop, conta) — TODOS
  deram 5002 "o número do contrato deve ser o mesmo número do cliente". Lição: NÃO é chutável,
  é dado do Sicoobnet→Cobrança. **Emissão Sicoob travada nisso** (Pedro vai pegar o nº certo).
- **BUGS RECORRENTES:**
  - openssl 3 + A1 ICP-Brasil: erro `RC2-40-CBC unsupported` (parece "senha errada", não é) → `-legacy`.
  - Management API Supabase devolve HTML (Cloudflare/rate-limit) sob muitos calls → esperar/retry.
  - Chamar edge do RisenOS direto exige `apikey`/anon ALÉM do HMAC, senão 401 do gateway.
  - Types estritos (SuperSec) quebram `.from(tabela nova)` → cast até regenerar typegen.
- **RESOLVIDOS:** `-legacy` (openssl), retry (Management API), header anon, cast (types).
  **AINDA ESPREITA:** numeroCliente Sicoob (bloqueia emissão de boleto pelo Sicoob).

## PRESENTE (onde estamos)
- Extrato Sicoob: **pronto/no ar**. Boleto Sicoob: **motor pronto, emissão travada** no numeroCliente.
- Conectores: **3/4 apps** (Hub, RisenOS, SuperSec) — falta **Ei Risen**.
- Pedido CRM→RisenOS: **backend pronto+provado**; falta painel no CRM + pagamento + volta OS→CRM.
- Concierge Bridge: **provider pronto+provado**; falta o consumidor (endpoint financaspedro + braço local 24/7).

## FUTURO (pra onde vamos)
- **Ei Risen** = último conector (liga o painel "Novo pedido", fechando o ciclo do pedido).
- **Consumidores do Concierge**: financaspedro chamar o inbox + braço local (prompt de handoff já entregue).
- **Sicoob boleto**: Pedro pega o numeroCliente de cobrança no Sicoobnet → destrava emissão + teste R$1.
- **Pagamento no pedido** (Pix/boleto/link Cora, caps já existem) + baixa automática.
- **Rumo geral:** costurar a frota via Core — barramento + conectores opt-in + concierge —
  tudo stateless, propagando padrões (Hub→RisenOS→SuperSec→eirisen). Medição desde já, preço depois.
</content>
</invoke>
